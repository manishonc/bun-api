{
  "name": "hello-world-api",
  "version": "1.0.0",
  "description": "Simple hello world API server",
  "main": "src/index.ts",
  "scripts": {
    "start": "bun run src/index.ts",
    "build": "bun build --compile src/index.ts --outfile ./dist/api",
    "db:migrate": "bun run src/db/migrate.ts"
  },
  "keywords": ["api", "bun", "server"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "bun-types": "^1.3.3"
  },
  "dependencies": {
    "postgres": "^3.4.7"
  }
}
